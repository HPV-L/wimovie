import React from 'react'
import { toast } from 'react-hot-toast'

export const metadata = {
    title: `Wimovie | Movie`
}

const page = () => {
  return (
    <div>
      Movie
    </div>
  )
}

export default page